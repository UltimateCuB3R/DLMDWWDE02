apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432

    user: postgres

    secureJsonData:
      password: mysecretpassword

    jsonData:
      database: postgres
      sslmode: disable

    isDefault: true